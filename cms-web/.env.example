# Backend API base URL — used by the fetch wrapper in src/lib/api/client.ts.
# Browser fetch sends `credentials: include` so the auth cookie flows.
# Backend must allow this origin in coach.cors.allowed-origins.
NEXT_PUBLIC_API_BASE=http://localhost:8080
